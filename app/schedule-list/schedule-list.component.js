// Register the  component on the  module,
angular.
	module('scheduleList').
	component('scheduleList', {  
		templateUrl: 'schedule-list/schedule-list.template.html',
		controller: ['$http','$scope',function ScheduleListController($http,$scope) {
			var self=this;
    	}]
	});
